import React from "react";

export default function Logout() {
  const handle = () => {
    alert("この機能はまだ実装されていません。");
  };

  return (
    <div>
      <button
      // onClick={
      //   handle
      // }
      >
        ログアウト
      </button>
    </div>
  );
}
