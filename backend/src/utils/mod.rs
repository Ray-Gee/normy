// use chrono::{DateTime, Utc};
// use chrono_tz::{Tz, Asia::Tokyo};

// pub fn to_jst<T: chrono::TimeZone>(dt: DateTime<T>) -> DateTime<Tz> {
//     dt.with_timezone(&Tokyo)
// }

// pub fn now_jst() -> DateTime<Tz> {
//     Utc::now().with_timezone(&Tokyo)
// }
